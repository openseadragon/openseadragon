*.sublime-workspace
node_modules
build/
sftp-config.json
coverage/
instrumented/
.idea
/nbproject/private/
.directory
local-test
.DS_Store

# Package managers (we use Bun, so ignore other lockfiles)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Note: bun.lockb is NOT ignored - it should be committed for reproducible builds
